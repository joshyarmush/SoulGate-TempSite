// Site configuration
export const siteConfig = {
  // Display mode: 'letterbox' or 'fullscreen'
  // letterbox: Shows full image with dark borders on sides/top/bottom as needed
  // fullscreen: Fills entire viewport, may crop parts of the image
  displayMode: 'letterbox' as 'letterbox' | 'fullscreen',
}